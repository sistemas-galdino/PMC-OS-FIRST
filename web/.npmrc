# AI SDK v4 declara peer zod@^3, mas o projeto usa zod@4 (o cliente nunca passa
# schemas zod pro pacote `ai` — só a Edge Function usa zod, no Deno, em separado).
# legacy-peer-deps evita o ERESOLVE no npm install local e no build da Vercel.
legacy-peer-deps=true
