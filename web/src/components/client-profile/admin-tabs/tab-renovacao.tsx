import { EmptyTabPlaceholder } from "./_placeholder"

export default function TabRenovacao(_props: { clientId: string }) {
  return (
    <EmptyTabPlaceholder
      titulo="Renovação"
      descricao="Status da renovação do contrato — data, probabilidade, condições negociadas e próximos passos."
    />
  )
}
