import { EmptyTabPlaceholder } from "./_placeholder"

export default function TabCancelamento(_props: { clientId: string }) {
  return (
    <EmptyTabPlaceholder
      titulo="Cancelamento"
      descricao="Registro de pedidos de cancelamento, motivos, status e ações tomadas."
    />
  )
}
